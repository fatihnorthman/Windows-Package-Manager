#include "Logger.h"
// Implementation lives entirely in the header (template-based log helpers).
// This translation unit exists to satisfy CMake target_sources.
namespace pm {
namespace {
// Intentionally empty. Logger.h is the single source of truth.
}
} // namespace pm
